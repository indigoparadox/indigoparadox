divert(-1)
/* vim: set syntax=javascript: */
changecom()
changequote(`[', `]') 
include([iwz_html.m4])
divert(0)

$(document).ready( function() {
   /*
   $('video').css( 'opacity', 0 );
   $('img').css( 'opacity', 0 );

   $('video').load( function() {
      $(this).animate( { 'opacity': 1 } );
   } );
   $('img').load( function() {
      $(this).animate( { 'opacity': 1 } );
   } );
   */
} );

